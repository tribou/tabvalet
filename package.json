{
  "name": "tabvalet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "docker run --rm --init -v $(pwd):/work -w /work --ipc=host mcr.microsoft.com/playwright:v1.60.0-noble xvfb-run npx playwright test -c tests/playwright.config.js",
    "test:e2e:ui": "playwright test -c tests/playwright.config.js --ui"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "canvas": "^3.2.3",
    "jest-chrome": "^0.8.0",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.7"
  }
}
